{
  "name": "@autokestra/plugin-runtime",
  "version": "0.0.1",
  "description": "Plugin runtime execution engine",
  "main": "index.ts",
  "scripts": {
    "build": "bun build index.ts --outdir dist",
    "lint": "bunx eslint src/**/*.ts",
    "format": "bunx prettier --write src/**/*.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@autokestra/plugin-sdk": "workspace:*",
    "js-yaml": "^4.1.0",
    "ajv": "^8.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}