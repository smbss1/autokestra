{
  "name": "@autokestra/engine",
  "version": "0.0.1",
  "description": "Core workflow engine logic",
  "main": "index.ts",
  "scripts": {
    "build": "bun build index.ts --outdir dist",
    "lint": "bunx eslint src/**/*.ts",
    "format": "bunx prettier --write src/**/*.ts"
  },
  "dependencies": {
    "valibot": "^1.2.0",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}