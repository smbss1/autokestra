{
  "name": "@autokestra/server",
  "version": "0.0.1",
  "description": "HTTP server and API layer",
  "main": "index.ts",
  "scripts": {
    "build": "bun build --target=bun index.ts --outdir dist",
    "lint": "bunx eslint src/**/*.ts",
    "format": "bunx prettier --write src/**/*.ts"
  },
  "dependencies": {
    "@autokestra/engine": "workspace:*",
    "@autokestra/secrets": "workspace:*",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}