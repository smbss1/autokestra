{
  "name": "@autokestra/secrets",
  "version": "0.0.1",
  "description": "Encrypted local secret store for workflows",
  "main": "index.ts",
  "scripts": {
    "build": "bun build index.ts --outdir dist",
    "lint": "bunx eslint src/**/*.ts",
    "format": "bunx prettier --write src/**/*.ts",
    "test": "bun test"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}