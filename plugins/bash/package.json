{
  "name": "@autokestra/plugin-bash",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "manifest:generate": "bun run ./generate-manifest.ts",
    "manifest:check": "bun run ./generate-manifest.ts && git diff --exit-code -- plugin.yaml"
  },
  "dependencies": {
    "@autokestra/plugin-sdk": "0.0.2",
    "valibot": "^1.2.0",
    "yaml": "^2.8.1"
  }
}
